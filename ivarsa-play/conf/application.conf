# This is the main configuration file for the application.
# ~~~~~
application.context="/api"
# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
play.crypto.secret="changeme"

# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Database configuration
# ~~~~~ 
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
# db.default.driver=org.h2.Driver
# db.default.url="jdbc:h2:mem:play"
#ebean.default="models.*"

jdbc.driver=com.mysql.jdbc.Driver
jdbc.host=localhost
jdbc.commondb=iqueue
jdbc.username=iqueue_u
jdbc.password="iqueue_p@$sw0rD"
jdbc.url="jdbc:mysql://"${jdbc.host}/${jdbc.commondb}

play.evolutions.autoApply=true

db.default.driver=${jdbc.driver}
db.default.url=${jdbc.url}
db.default.username=${jdbc.username}
db.default.password=${jdbc.password}
#db.default.jndiName=DefaultDS
jpa.default=defaultPersistenceUnit

# Assets configuration
# ~~~~~
"assets.cache./public/stylesheets/bootstrap.min.css"="max-age=3600"